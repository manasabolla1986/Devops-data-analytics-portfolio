version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: java-cicd-demo
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx512m -Xms256m
      - SPRING_PROFILES_ACTIVE=dev
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
